kubectl get pod <pod-name> -o yaml                      -> yaml output of config
kubectl get pods                                        -> lists all the pods
kubectl rollout status deployment/service-a-deployment  -> monitor rollout 
